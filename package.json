{
  "name": "pixel-workplace",
  "version": "0.1.0",
  "scripts": {
    "postinstall": "npm install --prefix server && npm install --prefix client && NODE_ENV=production npm run build --prefix client"
  }
}
